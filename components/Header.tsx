import React from "react";
import Container from "./Container";

const Header = () => {
  return <header>
    <Container className="max-w-full">Header</Container>    
  </header>;
};

export default Header;
